# Guidelines for Morning Report

This skill generates a daily system and infrastructure health report for Gading, covering the K3s cluster, Docker containers, CPU/RAM, NAS usage, OpenClaw logs, and filtered Gmail inbox.

## Execution Flow
1. Call the `generate_morning_report` tool to execute the report generator script and retrieve the markdown report.
2. Deliver the generated report directly to Gading.
